# hellouser

This is a simple "Hello, user!" Docker image built using a Red Hat Universal Base Image (UBI) as the base. The image contains a text file with the message "Hello, User!" to demonstrate its functionality.